# AGENTS.md

Use `aidx` as the shared-instance prefix for database objects in this example.

- `aidx` means **AI Detection Index**.
- Keep table, function, view, and tile layer names aligned to the `aidx_*` convention.
